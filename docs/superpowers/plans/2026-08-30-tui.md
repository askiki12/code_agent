# TUI 终端界面实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `--interactive` 增加 rich 驱动的单线程 TUI（状态栏 + 流式对话区 + 输入栏），TTY 自动进入、非 TTY 回退纯文本；权限 ask 在输入栏确认。

**Architecture:** 新增运行时依赖 `rich`；新模块 `code_agent/tui.py`（`run_tui` + `format_*` 纯函数）；`Policy.check` 注入可选 `ask` 回调（缺省 `input()`），`AgentSession` 透传 `ask`；`cli.py` 拆出纯函数 `handle_command` 与 `_use_tui()` 分支。

**Tech Stack:** Python 3.11+，`rich>=13`（新增唯一运行时依赖），`pytest`。测试离线为主（格式化/斜杠/ask 注入纯函数 + run_tui 冒烟桩）。

## Global Constraints

- 禁止任何 agent 框架/SDK；`rich` 是终端渲染库，非框架，允许（需 ADR-019）。
- 布局：状态栏 + 对话区 + 输入栏（无工具侧栏/多面板）；单线程 rich Live，运行中输入栏禁用。
- `Policy.check(tool, arguments, interact, ask=None)`——`ask` 缺省 `input()`（纯文本行为不变）；TUI 注入渲染回调。
- `AgentSession.__init__` 新增 `ask` 参数；`_dispatch_subagent` 子会话透传 `ask=self.ask`。
- `handle_command(cmd, session, store) -> tuple[bool, list[str]]` 为纯函数（cli 与 tui 共用）。
- CLI：`--interactive` 在 `_use_tui()`（TTY 且无 `NO_TUI`）时进 TUI，否则纯文本；`--prompt`/`--list-sessions`/`--resume` 不变。
- 凭据纪律不变；测试全部离线，禁止真实 API key；每个任务结束必须测试通过并提交（不 rebase/改写历史）。

---

### Task 1: 依赖 —— 新增 `rich>=13` + `uv sync`

**Files:**
- Modify: `code_agent/pyproject.toml`（`[project] dependencies` 加 `rich`）
- Modify: `code_agent/uv.lock`（`uv sync` 自动更新）

**Interfaces:**
- Consumes: 无。
- Produces: `rich` 可在 uv 环境导入。

- [ ] **Step 1: 修改 pyproject.toml**

`code_agent/pyproject.toml` 的 `dependencies` 行：
```toml
dependencies = ["requests>=2.31", "rich>=13"]
```

- [ ] **Step 2: uv sync 安装**

Run: `uv sync`
Expected: 成功，`uv.lock` 更新（含 rich）

- [ ] **Step 3: 验证可导入 + 全量回归**

Run: `uv run python -c "import rich; print(rich.__version__)"`
Expected: 输出版本号
Run: `uv run pytest tests/ -q`
Expected: 全绿（219）

- [ ] **Step 4: 提交**

```bash
git add pyproject.toml uv.lock
git commit -m "build: 新增 rich 运行时依赖（TUI 前置，Task 1/7）"
```

---

### Task 2: 权限 ask 注入 —— `Policy.check(..., ask=None)` + AgentSession 透传

**Files:**
- Modify: `code_agent/code_agent/permissions.py`（`check` 加 `ask` 参数）
- Modify: `code_agent/code_agent/agent.py`（`__init__` 加 `ask`、`_run_tool`/`_dispatch_subagent` 透传）
- Modify: `code_agent/tests/test_permissions.py`（ask 注入用例）
- Modify: `code_agent/tests/test_agent.py`（dispatch 透传 ask 用例）

**Interfaces:**
- Consumes: 既有 `PermissionResult` / `Policy`。
- Produces:
  - `Policy.check(self, tool, arguments, interact=False, ask=None) -> PermissionResult`——`ask` 为 `Callable[[str], str] | None`；`ask` 为空时用内置 `input`。
  - `AgentSession.__init__(..., ask: Callable[[str], str] | None = None)`；`self.ask`。
  - `AgentSession._run_tool` 调 `policy.check(..., ask=self.ask)`。
  - `AgentSession._dispatch_subagent` 子会话传 `ask=self.ask`。

- [ ] **Step 1: 写失败测试**

追加到 `code_agent/tests/test_permissions.py`：

```python
def test_check_ask_uses_injected_callback():
    from code_agent.permissions import Policy
    p = Policy(ask=["run_command:*"])
    seen = []

    def fake_ask(prompt):
        seen.append(prompt)
        return "y"

    r = p.check("run_command", {"command": "echo hi"}, interact=True, ask=fake_ask)
    assert r.decision == "allow"
    assert seen and "[permission]" in seen[0]


def test_check_ask_default_uses_input(monkeypatch):
    from code_agent.permissions import Policy
    p = Policy(ask=["run_command:*"])
    monkeypatch.setattr("builtins.input", lambda prompt: "n")
    r = p.check("run_command", {"command": "echo hi"}, interact=True)
    assert r.decision == "deny"
```

追加到 `code_agent/tests/test_agent.py`：

```python
def test_agent_dispatch_passes_ask_to_sub(workdir):
    from code_agent.permissions import Policy
    ask_calls = []

    def fake_ask(prompt):
        ask_calls.append(prompt)
        return "y"

    run = LLMResponse(
        content="",
        tool_calls=[ToolCall(id="s1", name="run_command", arguments={"command": "echo hi"})],
    )
    dispatch = LLMResponse(
        content="",
        tool_calls=[ToolCall(id="c1", name="dispatch_subagent", arguments={"task": "sub task"})],
    )
    llm = FakeLLM([dispatch, run, LLMResponse(content="sub done", tool_calls=[]), LLMResponse(content="done", tool_calls=[])])
    session = AgentSession(
        workdir=workdir, llm=llm, max_iterations=10,
        policy=Policy(ask=["run_command:*"]), interact=True, ask=fake_ask,
    )
    result = session.run_task("main task")
    assert result.finished and result.final_text == "done"
    assert ask_calls and "[permission]" in ask_calls[0]
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_permissions.py -k "ask" tests/test_agent.py -k "ask" -v`
Expected: FAIL（`TypeError: check() got an unexpected keyword argument 'ask'`）

- [ ] **Step 3: 最小实现**

`code_agent/code_agent/permissions.py` `check` 签名与 ask 分支改为：

```python
    def check(self, tool: str, arguments: dict, interact: bool = False, ask=None) -> PermissionResult:
        text = _match_text(tool, arguments)
        key = (tool, text)
        if self.would_loop(tool, arguments):
            self._calls.append(key)
            return PermissionResult("deny", "doom_loop detected: repeated tool call")
        if self._matches(self._deny, tool, text):
            self._calls.append(key)
            return PermissionResult("deny", "denied by rule")
        if self._matches(self._ask, tool, text):
            if interact:
                asker = ask if ask is not None else input
                try:
                    decision = "allow" if asker(
                        f"[permission] {tool}({text[:60]!r}) allowed? [y/N] "
                    ).strip().lower() == "y" else "deny"
                except (EOFError, OSError):
                    decision = "deny"
            else:
                decision = "deny"
            self._calls.append(key)
            return PermissionResult(decision, "ask rule")
        if self._matches(self._allow, tool, text):
            self._calls.append(key)
            return PermissionResult("allow")
        if tool == "run_command" and _is_readonly_command(text):
            self._calls.append(key)
            return PermissionResult("allow", "readonly whitelist")
        self._calls.append(key)
        return PermissionResult("allow")
```

`code_agent/code_agent/agent.py`：
1) `__init__` 签名 `interact: bool = False,` 之后加 `ask: Callable[[str], str] | None = None,`；`self.interact = interact` 之后加 `self.ask = ask`。
2) `_run_tool` 的 `self.policy.check(...)` 改为：
```python
            result = self.policy.check(tc.name, tc.arguments, interact=self.interact, ask=self.ask)
```
3) `_dispatch_subagent` 子会话构造参数追加 `ask=self.ask,`。

- [ ] **Step 4: 运行确认通过**

Run: `uv run pytest tests/test_permissions.py -v tests/test_agent.py -v`
Expected: PASS
Run: `uv run pytest tests/ -q`
Expected: 全绿（219 + 本 Task 新增）

- [ ] **Step 5: 提交**

```bash
git add code_agent/permissions.py code_agent/agent.py tests/test_permissions.py tests/test_agent.py
git commit -m "feat: Policy.check 注入 ask 回调 + AgentSession 透传（TUI 前置，Task 2/7）"
```

---

### Task 3: cli 纯函数化 —— `handle_command` + `_use_tui`

**Files:**
- Modify: `code_agent/code_agent/cli.py`（`handle_command` 纯函数、`_handle_command` 薄包装、`_use_tui`、`--interactive` 分支占位）
- Modify: `code_agent/tests/test_cli.py`（handle_command / _use_tui 用例）

**Interfaces:**
- Consumes: 既有 `SessionStore`。
- Produces:
  - `handle_command(command, session, store) -> tuple[bool, list[str]]`——`/new`、`/list`、`/resume <id>`、`/exit`、未知命令；退出返回 `(False, [])`。
  - `_handle_command(command, session, store) -> bool`——薄包装：打印返回行，返回 keep（纯文本循环用，行为不变）。
  - `_use_tui() -> bool`——`sys.stdout.isatty()` 且 `NO_TUI` 未设。
  - `main()` 的 `--interactive` 分支：`if _use_tui(): from code_agent.tui import run_tui; run_tui(...)`（`run_tui` 本 Task 未定义，用 `try/except ImportError` 或注释占位——本 Task 用 `_use_tui()` 测试即可，接线在 Task 5）。

- [ ] **Step 1: 写失败测试**

追加到 `code_agent/tests/test_cli.py`：

```python
def test_use_tui_tty(monkeypatch):
    from code_agent.cli import _use_tui
    monkeypatch.setattr("sys.stdout.isatty", lambda: True)
    monkeypatch.delenv("NO_TUI", raising=False)
    assert _use_tui() is True


def test_use_tui_not_tty(monkeypatch):
    from code_agent.cli import _use_tui
    monkeypatch.setattr("sys.stdout.isatty", lambda: False)
    monkeypatch.delenv("NO_TUI", raising=False)
    assert _use_tui() is False


def test_use_tui_no_tui_env(monkeypatch):
    from code_agent.cli import _use_tui
    monkeypatch.setattr("sys.stdout.isatty", lambda: True)
    monkeypatch.setenv("NO_TUI", "1")
    assert _use_tui() is False


def test_handle_command_exit_and_unknown():
    from code_agent.cli import handle_command
    keep, out = handle_command("/exit", None, None)
    assert keep is False and out == []
    keep, out = handle_command("/bogus", None, None)
    assert keep is True and out == ["unknown command: /bogus"]
    keep, out = handle_command("/resume", None, None)
    assert keep is True and out == ["usage: /resume <session-id>"]


def test_handle_command_new_and_list(workdir, tmp_path):
    from code_agent.cli import handle_command
    from code_agent.session import SessionStore
    store = SessionStore(str(tmp_path / "sessions"))
    sid = store.create("t")

    class _S:
        def new_session(self):
            self.called = True

    s = _S()
    keep, out = handle_command("/new", s, store)
    assert keep is True and getattr(s, "called", False) and out == ["New session started."]

    keep, out = handle_command("/list", None, store)
    assert keep is True and out and sid in out[0]


def test_handle_command_resume(workdir, tmp_path):
    from code_agent.cli import handle_command
    from code_agent.session import SessionStore
    store = SessionStore(str(tmp_path / "sessions"))
    sid = store.create("t")

    class _S:
        def load_session(self, sid):
            self.loaded = sid

    s = _S()
    keep, out = handle_command(f"/resume {sid}", s, store)
    assert keep is True and s.loaded == sid and out == [f"Resumed session {sid}."]
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_cli.py -v`
Expected: FAIL（`ImportError: cannot import name 'handle_command'` 等）

- [ ] **Step 3: 最小实现**

`code_agent/code_agent/cli.py`：将现有 `_handle_command` 替换为 `handle_command` + 薄包装，并加 `_use_tui`：

```python
def handle_command(command: str, session, store: SessionStore) -> tuple[bool, list[str]]:
    parts = command.split(maxsplit=1)
    cmd = parts[0]
    out: list[str] = []
    if cmd == "/new":
        session.new_session()
        out.append("New session started.")
    elif cmd == "/list":
        for s in store.list_sessions():
            out.append(f"{s['id']}  {s['title'] or ''}  ({s['message_count']} msgs)")
    elif cmd == "/resume":
        sid = parts[1] if len(parts) > 1 else ""
        if not sid:
            out.append("usage: /resume <session-id>")
        else:
            try:
                session.load_session(sid)
                out.append(f"Resumed session {sid}.")
            except KeyError:
                out.append(f"session not found: {sid}")
    elif cmd == "/exit":
        return False, []
    else:
        out.append(f"unknown command: {cmd}")
    return True, out


def _handle_command(command: str, session, store: SessionStore) -> bool:
    keep, out = handle_command(command, session, store)
    for line in out:
        print(line)
    return keep


def _use_tui() -> bool:
    return bool(sys.stdout.isatty()) and os.environ.get("NO_TUI") is None
```

`main()` 的 `--interactive` 分支（在 `if args.prompt:` 之后、`if workspace is not None:` 之前）占位：
```python
    if _use_tui():
        from code_agent.tui import run_tui  # noqa: F401  (Task 5 落地接线)
        return 0
```
（本 Task 该分支实际不会命中——测试直接测 `_use_tui`；Task 5 替换为真实调用。）

- [ ] **Step 4: 运行确认通过**

Run: `uv run pytest tests/test_cli.py -v`
Expected: PASS
Run: `uv run pytest tests/ -q`
Expected: 全绿

- [ ] **Step 5: 提交**

```bash
git add code_agent/cli.py tests/test_cli.py
git commit -m "refactor: cli 拆出 handle_command 纯函数 + _use_tui 分支（Task 3/7）"
```

---

### Task 4: tui.py —— 格式化纯函数

**Files:**
- Create: `code_agent/code_agent/tui.py`（本 Task 仅 `format_*` + 模块头部）
- Create: `code_agent/tests/test_tui.py`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `format_user(content: str) -> str`——`"> user: " + content`
  - `format_assistant(content: str) -> str`——`"assistant: " + content`
  - `format_tool(name: str, ok: bool, truncated: bool, output: str) -> str`——`[tool] <name> ok|failed[( truncated)]`，输出首行（若空不加 `|`）。

- [ ] **Step 1: 写失败测试**

`code_agent/tests/test_tui.py`：

```python
from code_agent.tui import format_assistant, format_tool, format_user


def test_format_user():
    assert format_user("hello") == "> user: hello"


def test_format_assistant():
    assert format_assistant("done") == "assistant: done"


def test_format_tool_ok_first_line():
    assert format_tool("read_file", True, False, "line1\nline2") == "[tool] read_file ok | line1"


def test_format_tool_failed_truncated():
    assert format_tool("run_command", False, True, "boom\nmore") == "[tool] run_command failed (truncated) | boom"


def test_format_tool_empty_output():
    assert format_tool("list_dir", True, False, "") == "[tool] list_dir ok"
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_tui.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'code_agent.tui'`）

- [ ] **Step 3: 最小实现**

`code_agent/code_agent/tui.py`（本 Task 先建模块，含 rich 顶层 import，供 Task 5 使用与测试 monkeypatch）：

```python
"""Rich terminal UI for interactive mode."""
from __future__ import annotations

from rich.console import Console
from rich.layout import Layout
from rich.live import Live
from rich.panel import Panel
from rich.text import Text


def format_user(content: str) -> str:
    return f"> user: {content}"


def format_assistant(content: str) -> str:
    return f"assistant: {content}"


def format_tool(name: str, ok: bool, truncated: bool, output: str) -> str:
    status = "ok" if ok else "failed"
    if truncated:
        status += " (truncated)"
    first = output.splitlines()[0] if output.strip() else ""
    return f"[tool] {name} {status}" + (f" | {first}" if first else "")
```

- [ ] **Step 4: 运行确认通过**

Run: `uv run pytest tests/test_tui.py -v`
Expected: PASS
Run: `uv run pytest tests/ -q`
Expected: 全绿

- [ ] **Step 5: 提交**

```bash
git add code_agent/tui.py tests/test_tui.py
git commit -m "feat: tui.py 对话/工具格式化纯函数（Task 4/7）"
```

---

### Task 5: tui.py —— `run_tui` + CLI 接线

**Files:**
- Modify: `code_agent/code_agent/tui.py`（追加 `run_tui`）
- Modify: `code_agent/code_agent/cli.py`（`--interactive` 分支真实接线）
- Modify: `code_agent/tests/test_tui.py`（run_tui 冒烟）

**Interfaces:**
- Consumes: Task 3 的 `handle_command`；Task 4 的 `format_*`；`rich.live.Live` / `rich.console.Console` / `rich.layout.Layout` / `rich.panel.Panel` / `rich.text.Text`。
- Produces:
  - `run_tui(session, store, workspace=None, *, model: str = "") -> None`——单线程 Live 循环：状态栏（workspace/model/session/state）+ 对话区（user/assistant/tool 行，流式 on_delta）+ 输入栏（普通任务 / 斜杠命令 / 运行中 `[running…]`）；权限 ask 用 `session.ask`（run_tui 开头设置为 Live-aware 回调）；`Ctrl+C`/`/exit` 退出。
  - `cli.main()`：`if _use_tui(): run_tui(session, store, workspace, model=llm.model); return 0`。

- [ ] **Step 1: 写失败测试**

追加到 `code_agent/tests/test_tui.py`：

```python
class _StubConsole:
    def __init__(self, lines):
        self._lines = list(lines)

    def input(self, prompt=""):
        return self._lines.pop(0)


class _StubLive:
    def __init__(self, *a, **k):
        pass

    def __enter__(self):
        return self

    def __exit__(self, *a):
        return False

    def update(self, x):
        pass

    def refresh(self):
        pass

    def start(self):
        pass

    def stop(self):
        pass


def test_run_tui_smoke(workdir, monkeypatch):
    from code_agent.agent import AgentSession
    from code_agent.llm import LLMResponse
    from code_agent.tui import run_tui

    class _FakeLLM:
        def __init__(self):
            self.calls = 0

        def chat(self, messages, tools=None, on_delta=None):
            self.calls += 1
            if on_delta:
                on_delta("streamed")
            return LLMResponse(content="done", tool_calls=[])

    llm = _FakeLLM()
    session = AgentSession(workdir=workdir, llm=llm, max_iterations=2)
    monkeypatch.setattr("code_agent.tui.Live", _StubLive)
    monkeypatch.setattr("code_agent.tui.Console", lambda **kw: _StubConsole(["hello", "exit"]))
    run_tui(session, store=None, workspace=None, model="m")
    assert llm.calls == 1
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_tui.py::test_run_tui_smoke -v`
Expected: FAIL（`AttributeError: module 'code_agent.tui' has no attribute 'run_tui'`）

- [ ] **Step 3: 最小实现**

`code_agent/code_agent/tui.py` 末尾追加：

```python
def run_tui(session, store, workspace=None, *, model: str = "") -> None:
    console = Console()
    conversation: list[str] = []

    def status_text(state: str) -> str:
        parts = []
        if workspace is not None:
            parts.append(workspace.display())
        parts.append(f"model: {model}")
        parts.append(f"session: {session.session_id or 'new'}")
        parts.append(state)
        return " | ".join(parts)

    def build_layout(state: str, prompt: str) -> Layout:
        body = "\n".join(conversation) if conversation else "(empty)"
        layout = Layout()
        layout.split_column(
            Layout(Text(status_text(state), style="bold cyan"), name="status", size=1),
            Layout(Panel(body, title="conversation"), name="conv"),
            Layout(Text(prompt), name="input", size=1),
        )
        return layout

    def append_delta(idx: int, delta: str, live: Live) -> None:
        conversation[idx] += delta
        live.refresh()

    with Live(console=console, screen=False, refresh_per_second=10) as live:
        def ask(prompt: str) -> str:
            live.stop()
            try:
                return console.input(prompt)
            except (EOFError, KeyboardInterrupt):
                return ""
            finally:
                live.start()

        session.ask = ask
        while True:
            live.update(build_layout("idle", "> "))
            live.refresh()
            live.stop()
            try:
                raw = console.input("> ")
            except (EOFError, KeyboardInterrupt):
                break
            finally:
                live.start()
            task = raw.strip()
            if not task:
                continue
            if task.lower() in {"exit", "quit"}:
                break
            if task.startswith("/"):
                keep, out = handle_command(task, session, store)
                conversation.extend(out)
                if not keep:
                    break
                continue
            conversation.append(format_user(task))
            idx = len(conversation)
            conversation.append("assistant: ")
            live.update(build_layout("running", "[running…]"))
            live.refresh()
            result = session.run_task(task, on_delta=lambda d: append_delta(idx, d, live))
            conversation[idx] = format_assistant(result.final_text) if result.final_text else conversation[idx]
            if not result.finished:
                conversation.append(f"[agent] stopped: {result.reason}")
            if session.session_id:
                conversation.append(f"[session {session.session_id}]")
```

`code_agent/code_agent/tui.py` 顶部 import 区补 `from code_agent.cli import handle_command`。

`code_agent/code_agent/cli.py` 的 `--interactive` 分支替换为真实接线：

```python
    if _use_tui():
        from code_agent.tui import run_tui
        run_tui(session, store, workspace, model=llm.model)
        return 0
```

- [ ] **Step 4: 运行确认通过**

Run: `uv run pytest tests/test_tui.py -v`
Expected: PASS
Run: `uv run pytest tests/ -q`
Expected: 全绿（含 cli 既有用例）

- [ ] **Step 5: 提交**

```bash
git add code_agent/tui.py code_agent/cli.py tests/test_tui.py
git commit -m "feat: tui.py run_tui + cli --interactive 接线（TTY 进 TUI，Task 5/7）"
```

---

### Task 6: 文档同步 + ADR-019

**Files:**
- Modify: `code_agent/docs/architecture.md`
- Modify: `code_agent/docs/development.md`
- Modify: `code_agent/docs/design.md`
- Modify: `code_agent/README.md`
- Modify: `../.agent/03-decisions.md`（工作区根，不入库）
- Modify: `../.agent/01-goals-and-boundaries.md` 与 `../.agent/04-engineering-standards.md`（依赖/测试范围同步，不入库）

**Interfaces:**
- Consumes: Task 1-5 的实现。
- Produces: 文档权威源与实现一致；ADR-019。

- [ ] **Step 1: architecture.md**

- 模块总览表加 `| tui.py | rich 终端界面：run_tui（状态栏/对话区/输入栏，单线程 Live）、format_* 格式化、权限 ask 渲染 | rich |`。
- §3 cli.py 加 `handle_command` / `_use_tui`；permissions.py 的 `check` 加 `ask` 参数；agent.py 加 `ask` 参数与透传说明。

- [ ] **Step 2: development.md**

- §1 环境：运行时依赖更新为 `requests` + `rich`。
- §2 运行：`--interactive` 说明更新——TTY 下进入 TUI（状态栏/对话区/输入栏、斜杠命令、运行中禁用输入、权限 ask 在输入栏确认）；`NO_TUI=1` 或非 TTY 回退纯文本。
- §3 测试目录：加 `test_tui.py`（格式化/run_tui 冒烟桩）。
- §6 演示准备：演示可用 TUI 交互展示（更直观）。

- [ ] **Step 3: design.md**

- §6 功能范围勾选：`- [x] TUI 终端界面（rich，--interactive 升级，状态栏+对话区+输入栏，ADR-019）`。
- §8 开发路线追加：`16. [x] 迭代增强：TUI 终端界面（ADR-019，设计见 docs/superpowers/specs/2026-08-30-tui-design.md）`。
- 更新测试计数到实际值。

- [ ] **Step 4: README.md**

- 环境依赖：运行时依赖 `requests>=2.31` + `rich>=13`。
- 使用：`--interactive` 说明更新（TUI；`NO_TUI=1` 回退）。
- 项目结构加 `tui.py` 行。
- 测试计数更新到实际值。

- [ ] **Step 5: .agent/03-decisions.md（工作区根）**

追加 ADR-019（格式沿用前序）：

```markdown
## ADR-019：TUI 终端界面（rich，--interactive 升级）
- **日期**：2026-08-30
- **状态**：已实施
- **背景**：agent 功能完善但交互仍是纯 input() 循环，观感朴素，不利于演示与日常使用；业界以 TUI 承载交互。
- **选项**：rich（渲染库，新依赖）/ stdlib curses 零依赖 / textual 完整框架；--interactive 升级 / 新增 --tui / --text；单线程 / 双线程 / 异步。
- **决策**：新增运行时依赖 `rich>=13`（终端渲染库，非 agent 框架，符合红线）；新模块 `tui.py`（run_tui 单线程 rich Live，状态栏+对话区+输入栏）；`--interactive` TTY 自动进 TUI、非 TTY 或 `NO_TUI=1` 回退纯文本；`Policy.check` 注入可选 `ask` 回调（缺省 input()，纯文本不变），TUI 在输入栏渲染权限确认；`cli.handle_command` 纯函数化（cli/tui 共用）。
- **理由**：rich 单一明确依赖、视觉精致适合演示；单线程 Live 简单可靠；ask 注入保持纯文本行为不变且可测。
- **影响**：运行时依赖 +1；`--interactive` 交互升级；可测纯函数（format_*/handle_command/ask 注入）。
```

- [ ] **Step 6: .agent/01 / .agent/04 同步（工作区根，不入库）**

- `.agent/01-goals-and-boundaries.md` §4 In scope：CLI 行更新为"`--interactive`（TTY 下 rich TUI，可回退纯文本）"。
- `.agent/04-engineering-standards.md` §4 必测范围加 `test_tui.py`；§2 依赖最小化说明补"`rich`（TUI 渲染，ADR-019）"。

- [ ] **Step 7: 运行全量测试 + 提交**

Run: `uv run pytest tests/ -q`
Expected: 全绿（实际总数以运行结果为准，文档写实际值）
```bash
git add code_agent/docs/ README.md
git commit -m "docs: 同步 TUI 文档并记录 ADR-019（架构/工具/设计/开发/README）"
```

---

### Task 7: 全量回归 + 真实冒烟 + 凭据复核

**Files:**
- 无（验证与收尾）。

**Interfaces:**
- Consumes: Task 1-6 全部实现。

- [ ] **Step 1: 全量测试**

Run: `uv run pytest tests/ -v`
Expected: 全绿

- [ ] **Step 2: CLI --help 正常**

Run: `uv run python -m code_agent --help`
Expected: 正常输出

- [ ] **Step 3: TUI 冒烟（真实 API，需 TTY 交互）**

经用户确认后运行（TTY 下）：
```bash
uv run python -m code_agent --interactive
```
Expected: 进入 TUI；提交一个真实任务（如"列出当前目录"），观察流式对话、`[tool]` 行与状态栏；`/exit` 退出恢复终端。若当前环境无 TTY，用 `script` 伪 TTY 包裹验证，或由用户在实际终端运行并反馈。

- [ ] **Step 4: 凭据复核**

Run: `git grep -iE "sk-[a-zA-Z0-9]{10,}|api[_-]?key\s*[:=]\s*['\"]?[a-zA-Z0-9]{16,}"`
Expected: 无命中

- [ ] **Step 5: 收尾确认**

- 文档权威源与代码同步（rich 依赖、TUI 说明、计数实际值）。
- `git status` 干净；提交历史完整（本迭代 7 个 commit + 可能修复波，未改写历史）。
