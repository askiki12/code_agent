# code_agent 环境配置模板
# 复制为 .env 并填入真实值（.env 已 gitignore，不会入库）
# 系统环境变量优先于 .env；命令行参数优先于两者。

CODE_AGENT_BASE_URL=https://api.example.com/v1
CODE_AGENT_API_KEY=sk-your-api-key-here
CODE_AGENT_MODEL=gpt-4o-mini
